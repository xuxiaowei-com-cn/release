name: distroless-iptables-loong64

on:
  push:
    paths:
      - 'images/build/distroless-iptables/**'
      - '.github/workflows/distroless-iptables-loong64.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      DOCKER_REGISTRY: ${{ vars.DOCKER_REGISTRY || 'registry.cn-qingdao.aliyuncs.com' }}
      DOCKER_NAMESPACE: ${{ vars.DOCKER_NAMESPACE || 'xuxiaoweicomcn' }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
        with:
          platforms: loongarch64
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to Docker
        uses: docker/login-action@v4
        with:
          registry: ${{ env.DOCKER_REGISTRY }}
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build and push distroless-iptables image for loong64
        env:
          ARCH: loong64
          QEMUIMAGE: xfan1024/qemu-user-static
          QEMUVERSION: latest
          QEMUFLAGS:
          BASEIMAGE: ghcr.io/loong64/debian:trixie-slim
          DISTROLESS_BASEIMAGE: ghcr.io/loong64/debian
          REGISTRY: ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_NAMESPACE }}
          IMAGE_VERSION: v0.9.1
          GORUNNERIMAGE: ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_NAMESPACE }}/go-runner-loong64:v2.4.0-go1.26.2-bookworm.0
        run: |
          cd images/build/distroless-iptables
          make push
